# Mô hình lớn / nhị phân (không commit)
backend/classification_models/fasttext_model.bin

# Biến môi trường chứa secret
.env
backend/.env
**/.env.local

# Python
__pycache__/
*.py[cod]
*.egg-info/
.pytest_cache/
.venv/
venv/

# IDE
.idea/
.vscode/
*.swp

# OS
.DS_Store
Thumbs.db

# User uploads / procedure exports (local & Docker bind mount)
backend/user_data/

# Bản markdown cá nhân (backup khi đồng bộ corpus từ origin/main)
backend/markdown_data_local_backup/

# Cache / artifacts (tạo lại bằng script)
backend/cache/*.json
backend/cache/*.pkl
backend/cache/*.bin
*.log

# SQLite dev DB (Postgres trong Docker là nguồn chính)
backend/ragapp.db

# Cursor / tooling
.cursor/

# OS / editor
*.tmp
*.bak
